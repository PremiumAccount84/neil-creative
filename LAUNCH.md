# Notification settings

SMS signup is paused. To restore Text and Both options and the phone fields, set `smsNotifications: true` in `site-config.js` and deploy. Existing SMS form logic and consent remain in place. Verify the SMS delivery provider before enabling it. Email signup remains available.
